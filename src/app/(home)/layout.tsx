export default function UploadifyHomepage(props: {children: React.ReactNode}) {
  return (
    <div>
      {/* Hero Section */}
      <section>
        {props.children}
      </section>
    </div>
  )
}

